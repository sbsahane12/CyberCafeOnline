<%layout("/layouts/adminboilerplate")%>
    <style>
        /* Styles for the container */
        .max-w-lg {
            max-width: 32rem;
            /* Adjust the max-width as needed */
        }

        /* Styles for the heading */
        .text-lg {
            font-size: 1.125rem;
            /* Adjust the font size as needed */
        }

        .font-semibold {
            font-weight: 600;
        }

        /* Styles for the input fields */

        input[type="file"] {
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 0.5rem;
            border: 2px solid #f37a05;
            /* Adjust the border color as needed */
            border-radius: 0.375rem;

        }

        input[type="text"] {
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 0.5rem;
            border: 2px solid #f37a05;
            /* Adjust the border color as needed */
            border-radius: 0.375rem;
            /* Adjust the border radius as needed */
        }

        input[type="password"] {
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 0.5rem;
            border: 2px solid #f37a05;
            /* Adjust the border color as needed */
            border-radius: 0.375rem;
            /* Adjust the border radius as needed */
        }


        .width-full {
            width: 400px;
        }

        /* Styles for specific buttons */
        .bg-zinc-300 {
            background-color: #f2f5f7;
            /* Adjust the background color as needed */
        }

        .bg-zinc-400 {
            background-color: #dce5ec;
            /* Adjust the background color as needed */
        }

        .bg-red-500 {
            background-color: #e53e3e;
            /* Adjust the background color as needed */
        }

        .bg-red-600 {
            background-color: #c53030;
            /* Adjust the background color as needed */
        }

        .bg-orange-500 {
            background-color: #f37a05;
            /* Adjust the background color as needed */
        }

        .orange-500 {
            color: #f37a05;
            /* Adjust the background color as needed */
        }


        .bg-orange-600 {
            background-color: #ed6707;
            /* Adjust the background color as needed */
        }

        .text-black {
            color: #000;
            /* Adjust the text color as needed */
        }

        .text-white {
            color: #fff;
            /* Adjust the text color as needed */
        }

        /* Hover styles for buttons */

        .update {
            margin-left: -2px;
            margin-top: 6px;
            margin-bottom: 6px;
        }

        .flex {
            display: flex;
        }

        .items-center {
            align-items: center;
        }

        .justify-center {
            justify-content: center;
        }

        .rounded-full {
            border-radius: 9999px;
            height: 100px;
            width: 100px;
            border: #ed6707 2px solid;
        }

        .borderBox {
            border: #ed6707 2px solid;
            border-radius: 20px;
        }

        .signup {
            font-size: 25px;
            color: #f37a05;
            font-weight: 600;
            margin-top: 10px;
            margin-bottom: 10px;

        }
    </style>

    <body>
        <form class="mb-4 mx-auto width-full needs-validation mb-4 mt-4" action="/admin/resetPassword" method="POST"
            novalidate>
            <% if (error.length> 0) { %>
                <p>
                    <%= error %>
                </p>
                <% } %>
                    <div class="max-w-lg mx-auto p-4 bg-white shadow-lg rounded-lg ">

                        <h2 class="text-lg items-center justify-center flex mt-0 orange-500 signup"> Reset Password</h2>
                        <div class="grid grid-cols-1 gap-4 ">

                            <label for="email">Email</label>
                            <input type="text" name="email" id="email" class="form-control border-orange-500 rounded"
                                required>
                            <label for="password">Password</label>
                            <input type="password" name="password" id="password"
                                class="form-control border-orange-500 rounded" required>
                            <label for="confirmPassword">Confirm Password</label>
                            <input type="password" name="confirmPassword" id="confirmPassword"
                                class="form-control border-orange-500 rounded" required>
                            <input type="hidden" name="token" value="<%= token %>">
                            <button class="btn">Submit</button>
                            <a class="btn" href="/user/login">Cancel</a></p>

                        </div>

                    </div>

        </form>

        <script>

            (function () {
                'use strict'

                // Fetch all the forms we want to apply custom Bootstrap validation styles to
                var forms = document.querySelectorAll('.needs-validation')

                // Loop over them and prevent submission
                Array.prototype.slice.call(forms)
                    .forEach(function (form) {
                        form.addEventListener('submit', function (event) {
                            if (!form.checkValidity()) {
                                event.preventDefault()
                                event.stopPropagation()
                            }

                            form.classList.add('was-validated')
                        }, false)
                    })
            })()
        </script>

    </body>